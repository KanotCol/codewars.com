def row_sum_odd_numbers(n):
    if n == 1: return 1
    valIni, num, suma = 4, 5, 0
    for i in range(2, n):
        valIni += 2
        num += valIni
    for i in range (n):
        suma += num
        num -= 2
    return suma
