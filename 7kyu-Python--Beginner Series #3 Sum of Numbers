def get_sum(a, b):
    suma = 0
    if a == b:
        return (a)
    else:
        if a > b:
            a, b = b, a
        while a <= b:
            suma = suma + a
            a += 1
        return(suma)    
