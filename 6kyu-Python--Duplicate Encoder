def duplicate_encode(word):
    word, cad = word.upper(), ''
    for i in range(len(word)):
        if word.count(word[i]) > 1:
            cad += ')'
        else:
            cad += '('
    return(cad)
